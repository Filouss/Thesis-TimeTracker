name: Wake Up Render
on:
  schedule:
    - cron: '0 7 * * *' 
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Backend
        run: curl -s https://time-tracker-api-0any.onrender.com/actuator/health
